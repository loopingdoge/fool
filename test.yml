# Sintassi

# nometest:
# - codice
# - risultato (stringa)

test1:
-   let
      int y = 5 ;
    in
      print( y );

-   "5"

test2:
-   let
      int y = 5 + 2;
    in
      print( y );
-   "7"

test3:
-   let
        int x = 5;
        int f ()
            let
              int g()
                x
              ;
            in
              g()
        ;
    in
        print(f());
-   "5"

test4:
-   let
        int y = 5 + 2 ;
        bool f (int n, int m)
            let
                int x = m;
            in
                x == y;
    in
        print(if f(y, 8) then {false} else {10});
-   "Error: \"Incompatible types in then else branches\" at line 1, column 77."

test5:
-   let
        int geee(int n)
            n + 1;
        int foo(int n)
            if (n == 0) then { 1 } else { n * geee(n-1) };
    in
        print(foo(3));
-   "9"

test6:
-   let
        int foo(int n)
            let
                int gee(int n) n+1;
            in
                if (n==0) then { 1 } else { n * gee(n-1) };
    in
        print(gee(3));
-   "Id gee not declared"

test7:
-   print(1);
-   "1"

# Test one method without params call
test8:
-   class Numero {
        int uno() 1;
    };
    let
        Numero num = new Numero;
    in
        print(num.uno());
-   "1"

# Test two method without params calls
test9:
-   class Numeri {
        int uno() 1;
        int due() 2;
    };
    let
        Numeri num = new Numeri;
    in
        num.uno() + num.due();
-   "3"
