let
    int x = 5;
    int f ()
        x;
in
    f(x);

/* Altri esempi

let
    int y = 5 + 2 ;
    bool f (int n, int m)
    let
        int x = m;
    in
        x==y;
in
    print(if f(y, 8) then {false} else {10});

*/
